## can_ex02_transfer

### 1 概述
本例程展示了CAN数据传输功能，通过中断方式接收外部帧并发送相同帧。

### 2 准备
ALB32R0039_EVM开发板
GPIO4 -> 数据收发器的TX
GPIO5 -> 数据收发器的RX

### 3 运行步骤
1.配置CAN模块
2.配置中断处理函数
3.启动CAN模块
4.启用自动重传功能

### 4 运行现象
PC向CAN1发送的帧都能被返回，且程序可以进入中断。
