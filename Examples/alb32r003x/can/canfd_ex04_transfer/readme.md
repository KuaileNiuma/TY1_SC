## canfd_ex04_transfer

### 1 概述
本例程展示了CAN FD模式下，通过中断方式完成CAN FD数据的接收和转发。

### 2 准备
ALB32R0039_EVM开发板
GPIO4 -> 数据收发器的TX
GPIO5 -> 数据收发器的RX

### 3 运行步骤
1.配置为CAN FD模式
2.配置为中断方式接收和转发
3.启用自动重传功能

### 4 运行现象
PC向CAN1发送的帧都能被返回，且程序可以进入中断
