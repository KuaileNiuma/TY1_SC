## clb_ex08_timer_two_states

### 1 概述
本例程展示了CLB结合两种状态timer实现的功能。

### 2 准备
ALB32R0039_EVM开发板
使用示波器连接GPIO0

### 3 运行步骤
使用FSM子模块来切换 CLB 的输出，然后将其输出到 GPIO。 FSM 模块充当单比特存储块。中断设置与前一个示例的格式相同。

### 4 运行现象
通过比较CLB 的输出和在中断服务例程中切换的 GPIO 的输出，可以看到 CLB 的中断延迟。 
捕获到的波形图如图5-12以及5-13所示。
