## clb_ex11_push_pull

### 1 概述
本例程展示了CLB HLC模块通过PULL指令来更新COUNTER事件触发的频率的功能。

### 2 准备
ALB32R0039_EVM开发板
使用示波器连接GPIO22、GPIO7、GPIO24。

### 3 运行步骤
板级初始化 -> 初始化CLB -> 在主循环中每2s执行一次完整的" 数据写入、触发加载、清空 " 过程。

### 4 运行现象
GPIO22,GPIO7,GPIO24输出的PWM波频率更新后为更新前的一半。捕获到的波形图如图5-16以及图5-17所示。
