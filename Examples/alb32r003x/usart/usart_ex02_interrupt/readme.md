## usart\_ex02\_interrupt

### 1 概述

本例程展示了USART在中断中收发数据功能

### 2 准备

ALB32R0039\_EVM开发板

GPIO22 -> 串口RX
GPIO23 -> 串口TX

### 3 运行步骤

软件执行UART2以中断的方式接收数据。
具体流程为：开启USART1→配置GPIO引脚→配置USART中断→注册中断处理函数→在中断中发送和接收数据。

### 4 运行现象

使用串口给USART1发送数据，程序可以接收数据并进入中断，并将数据回显到串口。

