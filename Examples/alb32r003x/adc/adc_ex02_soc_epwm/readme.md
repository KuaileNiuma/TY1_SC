## adc_ex02_soc_epwm

### 1 概述
本例程展示了使用epwm来触发ADC进行电压采样的功能

### 2 准备
ALB32R0039_EVM开发板

### 3 运行步骤
使用ePWM模块周期性生成SOCA事件信号触发ADC转换序列。
ADC配置转换触发源为ePWM的SOC事件，在转换结束中断服务程序(ISR)中将转换结果存储到缓冲区数组。
当缓冲区填满后设置标志位，主循环检测到标志位后禁用ePWM模块停止SOC事件信号。
具体流程为：ePWM周期性触发ADC转换→中断中存储结果→缓冲区满置标志位→主循环停用ePWM。

### 4 运行现象

```

```