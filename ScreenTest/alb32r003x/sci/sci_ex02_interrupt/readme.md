## sci_ex02_interrupt

### 1 概述

本例程展示了使用UART中断接收和发送数据功能

### 2 准备

ALB32R0039\_EVM开发板

GPIO10 ->	串口的RX
GPIO11 ->	串口的TX

### 3 运行步骤

软件执行UART2以中断的方式接收数据。
具体流程为：开启UART2→配置GPIO引脚→配置UART中断→注册中断处理函数→等待接收数据。

### 4 运行现象

使用串口给UART2发送数据，程序可以接收数据并进入中断，且发送和接收的数据一致。

